// 77442 Volodymyr Doroshevych - zad5
const form=document.querySelector('#contactForm'),result=document.querySelector('#result');function noDigits(v){return !/\d/.test(v)}function validEmail(v){return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(v)}form.addEventListener('submit',e=>{e.preventDefault();let errors=[];const fn=firstName.value.trim(),ln=lastName.value.trim(),em=email.value.trim(),msg=message.value.trim();if(!fn)errors.push('Imię jest wymagane.');if(fn&&!noDigits(fn))errors.push('Imię nie może zawierać cyfr.');if(!ln)errors.push('Nazwisko jest wymagane.');if(ln&&!noDigits(ln))errors.push('Nazwisko nie może zawierać cyfr.');if(!em)errors.push('E-mail jest wymagany.');else if(!validEmail(em))errors.push('Adres e-mail ma niepoprawny format.');if(!msg)errors.push('Wiadomość jest wymagana.');result.innerHTML=errors.length?'<ul class="error"><li>'+errors.join('</li><li>')+'</li></ul>':'<p>Dane formularza są poprawne.</p>';});
